# Thinking Process
- 2026-04-24-1010: 
    - กำลังจะสร้าง `index.js` ตามโฟลเดอร์สตรัคเจอร์ของคุณกัน ตอนนี้คิดว่าจะร่างตามเท่าที่คุณกันเปิดโชว์ไว้ก่อน เดี๋ยวต้องไปทำความเข้าใจ `PromptSync`

```js
const myField = new Field(Field.generateField(3, 3, 0.2));
myField.runGame();
```

- 2026-04-24-1023: 
    - จากโค้ดด้านบน คิดว่าต้องสร้าง method ภายใน class `Field` กลับไปดู OOP ที่เคยทำแล้ว แต่สงสัยว่าทำไมส่ง method เข้าไปใน parameter 🤔
    - ไปหาคำตอบมา คิดว่าน่าจะเป็นการใช้ static method แปลว่าตอนสร้าง class `Field` เราต้องสร้างเมธอดนี้แบบ static บวกกับสร้างเมธอด `runGame` สำหรับ ไว้เรียกใช้
    - เท่าที่ดูจาก parameters ที่คุณกันใส่ไว้ให้ดู ใน `generateField` คือ metrix ความกว้าง ความสูง แล้วก็ เปอร์เซ็นต์ของกับดัก
- 2026-04-24-1108:
    - จะสร้าง array ให้เป็นหลาย ๆ แถวยังไง -> ไปศึกษา 2D Arrays, Nested Arrays, Nested Loops, Array Methods, Math.random() & Logic, String Manipulation
- 2026-04-24-1121:
    - ผมให้ AI ช่วยสร้างโจทย์เป็นสเต็ปสไตล์ codecademy จากนี้ไปจะแปะโจทย์แนบ step-by-step
    
```md
## Step 1: Skeleton & Printer 🏗️

### **Concept (แนวคิด)**
ก่อนจะเริ่มเดินได้ เราต้องมี "โลก" ของเกมก่อนครับ ในที่นี้คือการสร้าง `Class Field` เพื่อเก็บข้อมูลสนามในรูปแบบ **2D Array** (อาเรย์ซ้อนอาเรย์) และเราต้องมีวิธีการแสดงผล (Render) สนามนั้นออกมาให้ผู้เล่นเห็นใน Terminal แบบสวยงาม

### **Task (ภารกิจ)**
1.  สร้าง `Class Field` ในไฟล์ `index.js`
2.  เขียน `constructor` เพื่อรับค่าสนาม (Field Array) เข้ามาเก็บไว้ใน Instance
3.  เขียน Method ชื่อว่า `print()` เพื่อนำ Array 2 มิติมาแปลงเป็น String แล้ว `console.log()` ออกมา
```

- 2026-04-24-1124: 
    - `Field` สร้างไว้แล้ว
    - เปิด mdn ดูวิธีการเขียน constructor
- 2026-04-24-1202:
    - กำลังคิดลอจิกการสร้าง array 2 มิติแบบสุ่ม ตอนนี้คิดว่าจะเอาผลลัพธ์ทั้งหมดมาก่อน คือ `width * height` แล้วก็ หาค่า จำนวนเต็มของ `trapPercent` 
    → แล้วจะสร้าง array 1 มิติ จำนวนเท่านั้นออกมา 
    → แล้วก็จะเปลี่ยนค่าแรก ๆ ของ arr ที่มีจำนวนไอเทมเท่าจำนวนเต็มของ `trapPercent` เป็น `hole` ให้หมด นอกนั้นเป็นแค่ `fieldCharacter` 
    → หลังจากนั้นจะเปลี่ยน `fieldCharacter` 2 ตัว เป็น `hat` และ `pathCharacter`
    → หลังจากนั้นก็จะหาทาง shuffle array 
    → หลังจากนั้นจะดึง `pathCharacter` ออกมา แล้วยัดไปข้างหน้าสุดของ array
    → แล้วใช้ loop เพื่อหั่น array มาต่อกันเป็น 2 มิติ
    → ลองดูก่อน ไม่ได้ค่อยปรับ
- 2026-04-24-1251:
    - จะใช้ `.sort(() => {Math.random()-0.5})` เลยต้องใช้เวลาทำความเข้าใจมาพักนึง พอจะเก็ตแล้ว
- 2026-04-24-1340:
    - สร้าง static function เสร็จแล้ว แต่ยังลองรันไม่ได้ ต้องทำ printer ก่อน
    - ต้องไปเรียน `.map()` → ศึกษาและนำไปใช้แล้ว คือ arr method ใช้ประมาณว่าเป็นการลูป โดยรับค่าเป็น function แล้วคืนค่ากลับมาเป็น arr
    - ใช้คู่กับ arr method `.join()` โดยรอบแรกเชื่อมด้วย `''` รอบสองเชื่อมด้วย `'\n'`

